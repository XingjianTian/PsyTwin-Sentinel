# 心宠 AI 双层实时联调演示设计

## 目标

- [x] Gemini Live 会话固定使用服务端配置的单一 Voice，并在令牌锁定配置中注入已保存的 PetAiProfile 与 OCEAN 画像；每次开始播放回答时由客户端强制执行一次按性格画像确定的固定实体动作，并在开始会话前停止旧 ClawBody 语音链路，同时通过 `play_sound=false` 关闭情绪动作库 `.ogg` 音效。*(已于 2026-08-05 修复会话配置被客户端覆盖、音色随机、动作依赖模型工具调用和双重语音输出的问题；实体动作待用户连机验收)*

- [x] 在实时联调页签增加 Gemini Live 实体心宠语音联调：服务端签发短时令牌，浏览器通过官方 Live SDK 发送 16-bit PCM/16 kHz 心宠麦克风音频，接收原生音频及输入/输出转写，并绑定实体心宠扬声器播放。*(已于 2026-08-05 完成服务端临时令牌、官方 SDK 会话、实体音频设备选择与测试学生限制；未找到实体音频设备时不回退到电脑扬声器)*
- [x] Gemini Live 回答首段音频到达时确定性触发实体心宠官方表情动作；动作选择读取已保存的心宠性格和 OCEAN 画像，执行经过 Sentinel/Host Bridge 双层白名单，并关闭动作库音效。*(已于 2026-08-05 替代历史 `play_pet_emotion` 工具调用，完成动作参数校验、错误回传和页面状态展示；实体动作待用户连机验收)*
- [x] Gemini Live 正常关闭码 `1000/1005` 不再被误报为“尚未完成会话初始化”；`setupComplete`、连接关闭和手动停止之间的竞态已安全收口。*(已于 2026-08-05 完成浏览器握手回归)*
- [x] Gemini Live 输入转写累计使用统一风险规则；判级前移除 ASR 插入的空格和标点，“我 买 上 总 是 睡 不 好 。”等真实转写会显示中风险，并同步当前学生列表、会话标签与预警工单。*(已于 2026-08-05 根据数据库真实转写完成分类回归)*
- [x] Gemini Live 启动时恢复耳朵连续左右摆动；Sentinel 白名单允许 Host Bridge `processing` 接口，等待阶段仅下发耳朵字段，以约 12°轻幅反向摆动，完全不触碰头部和身体，停止时仅耳朵回中且不播放动作库音频。*(已于 2026-08-05 完成 Host Bridge 115 项回归、服务重启和实体接口验收)*
- [x] Gemini Live 每轮输入/输出转写幂等写入真实对话记录，并以 `gemini-live` 标记来源；中高风险学生原话同步创建真实待处理预警工单。*(已于 2026-08-05 完成 API 与持久化读取回归)*
- [x] Gemini Live 协作过程由已保存消息生成可审核阶段：风险识别、咨询师关注队列、规则化专业建议、性格化心宠回应和原生语音播放；不展示模型原始思维链。*(已于 2026-08-05 完成事件构建与页面回显测试)*

在 `/pet-ai-management` 的“实时联调”页签中展示会议确定的双层 AI 链路。第一层心宠 AI 负责学生日常对话和个性化表达；检测到负面情绪后，把内容交给第二层“小芯 AI”专业咨询师演示层，再由心宠 AI 将专业建议转述给学生。实体链路继续使用百度 ASR/TTS 和阿里云 OpenAI 兼容模型。

## 产品边界

- [x] 测试学生 `stu-test` 在符合筛选条件时置顶，心宠名称固定并持久化为“测试心宠”。*(已于 2026-07-20 完成)*
- [x] 测试心宠不显示伪造演示历史；实体联调产生的学生原话与心宠回复会幂等保存到真实对话表，并在“对话记录”页签展示。*(已于 2026-07-23 完成实时保存与停止前最终同步)*
- [x] `PetAiProfile` 只配置第一层心宠 AI 的语气、回复风格、主动程度、身份约束和知识范围。*(已于 2026-07-20 完成服务端配置注入)*
- [x] `robot_identity/AGENTS.md` 提供第一层心宠 AI 的公共安全与实体心宠身体能力规则，不再把“小芯”作为实体心宠的固定名称。*(已于 2026-07-20 完成)*
- [x] 第二层“小芯 AI”仍是演示用专业咨询师角色，不接入真实六智能体；但实体心宠学生转写一旦判定为中风险、高风险或危机，会幂等写入真实预警工单系统。*(已于 2026-07-23 接通预警工单)*
- [x] 页面展示结构化处理事件，不展示或声称展示模型的原始思维链。*(已于 2026-07-20 完成)*

## 双层调用流程

1. 实体心宠通过百度 ASR 得到学生话语，并写入实时转写。
2. 对普通话语，第一层心宠 AI 直接使用当前已保存的 `PetAiProfile` 生成回复。
3. 对包含“心情不好”“不想吃饭”等演示触发词的负面话语，ClawBody 写入“检测到负面情绪”和“已转交小芯 AI”事件。
4. 小芯 AI 使用同一阿里云模型、独立专业提示词生成简短、可审核的支持建议。该结果是模拟专业智能体输出，不是诊断结论。
5. 第一层心宠 AI 根据 `PetAiProfile` 将建议转述为学生可直接听到的回复。
6. 百度 TTS 播放最终回复，页面事件流显示 TTS 状态。

## 提示词分层

- 公共基础层：`robot_identity/AGENTS.md`，负责安全边界、真实性、实体心宠身体与动作约束。
- 心宠个性层：服务端从 `PetAiProfile` 生成运行时提示词，浏览器不得直接提交或覆盖提示词。
- 小芯专业层：独立的演示咨询师提示词，只生成结构化建议，不直接面向学生说话。
- 心宠转述层：将原始学生话语和小芯建议交给第一层心宠 AI，生成最终口语回复。

## 实时联调界面

“实时联调”页签保留设备状态和开始/停止按钮，下方按时间显示两类内容：

- 实时对话：学生 ASR 文本与测试心宠最终回复，使用左右气泡区分。
- 协作过程：采用类似开发工具任务流的纵向事件列表，显示“负面情绪识别”“转交小芯 AI”“专业建议”“心宠转述”“TTS 播放”等状态和摘要。
- [x] 实时转写携带 `risk_level`，中风险使用琥珀色、高风险与危机使用红色；学生原话、对应心宠回复、协作过程和左侧学生风险标签保持一致。*(已于 2026-07-21 完成)*
- [x] 会话风险采用只升级策略，避免后续普通话语让风险标签立即闪回；停止会话后重新读取已持久化对话，并按学生原话的最高风险继续展示标签，不修改学生数据库风险等级。刷新后的新对话从最新游标继续检测。*(已于 2026-08-01 完成停止会话后的风险回填与回归测试)*
- [x] 演示对话记录按学生原句进行确定性风险判级，心宠回复继承对应学生消息等级；学习压力、睡眠困扰、焦虑和沟通回避显示中风险，自伤轻生表达显示高风险。*(已于 2026-07-21 完成)*

协作事件仍仅保存在 ClawBody 内存中，最多 100 条。对话转写由 Sentinel 轮询后按确定性消息 ID 幂等保存到 `ChatSession/ChatMessage`，停止会话前再执行一次最终同步，避免遗漏最后一句；学生原话被判定为中风险、高风险或危机时，还会按“学生 + 转写 ID + 时间 + 内容”生成确定性工单 ID，将风险等级、学生原话和建议干预方案写入 `WorkOrder`。重复轮询不会重复保存消息或建单，普通话语和心宠回复不写入工单。

## 错误处理

- ClawBody 不可用时保持现有 `503` 映射和重试入口。
- [x] 实体心宠连接、摄像头和音频初始化在后台线程执行，启动阶段返回 `starting`，不得阻塞 `/health`、状态轮询或 Sentinel 页面。*(已于 2026-07-20 完成并通过真实设备启动验证)*
- [x] 百度 TTS 单声道音频按实体心宠输出声道数转换，连续写入带时间戳的播放缓冲区后再等待整段时长，避免双声道解释错误、缓冲欠载和语音截断。*(已于 2026-07-20 完成并通过音频回归测试)*
- 小芯模型调用失败时记录失败事件，并使用明确标注的演示兜底建议继续完成心宠转述，保证现场演示不中断。
- 只有 `stu-test` 可以启动实体心宠；其他学生仍只能查看和保存心宠个性配置。
- 已保存配置在下一次实体心宠会话启动时生效；运行中的会话不热切换。

## 验收

- [x] “测试学生”置顶、显示“测试心宠”；无真实记录时显示空状态，完成实体联调后展示已持久化的学生原话与心宠回复。*(已于 2026-07-23 完成真实对话记录接通)*
- [x] 启动会话时服务端读取完整 `PetAiProfile`，浏览器请求不携带 `identity`。*(已于 2026-07-20 通过接口测试)*
- [x] 普通话语只出现学生与心宠对话，不触发小芯流程。*(已于 2026-07-20 通过 ClawBody 测试)*
- [x] “我今天心情不好，不想吃饭了”稳定触发情绪识别、转交、专业建议和心宠转述可视化事件。*(已于 2026-07-20 通过文本联调与浏览器验收)*
- [x] 中风险与高风险在实时气泡、协作过程和学生列表中形成可见且带文字标签的颜色联动。*(已于 2026-07-21 通过 Node 与 Python 回归测试)*
- [x] 中高风险学生原话会自动生成一条待处理预警工单，兼容实体心宠服务输出的 `user` 角色与页面演示数据的 `student` 角色；侧边栏“预警工单管理”使用小型泛光红点显示未查看新增数，进入工单页面后立即清零，之后仅在新数据进入时累加并单次跳动。*(已于 2026-07-23 完成 12 项聚焦测试、生产构建与浏览器点击清零验收，页面控制台无错误)*
- [x] 实时联调的学生原话与心宠回复会持续幂等写入真实对话记录，停止会话前补齐最后一批转写；重新打开“对话记录”或刷新页面后仍可查看。*(已于 2026-07-23 完成持久化与回归验证)*
- [x] 仅测试学生的“对话记录”提供带二次确认的清空入口；服务端再次校验测试学生身份，只删除实体心宠联调会话，不影响性格配置、预警工单或其他学生数据。*(已于 2026-07-23 完成权限限制与回归验证)*
- [x] 协作过程按事件 ID 倒序展示，最新阶段始终位于列表顶部；底层轮询、游标和去重顺序保持不变。*(已于 2026-07-21 完成不可变排序回归测试)*
- [x] 协作过程将专业层统一展示为“咨询师智能体”：转交阶段显示“转交咨询师智能体”，专业阶段显示“咨询师智能体专业建议”并复用心图·AI 助手的 `Therapist.png` 头像；兼容 ClawBody 已产生的旧标题。*(已于 2026-07-21 完成显示映射测试)*
- [x] 最终心宠回复已接入原有百度 TTS 与实体心宠动作代码链路。*(已于 2026-07-20 完成代码接通；实体硬件仍待连机验收)*
- [x] Gemini Live 语音试听与固定配置入口已记录：在 Google AI Studio 的 Voice Library 试听后，将官方 Voice 名称写入服务端 `GEMINI_LIVE_VOICE`，不在浏览器随机选择音色。*(已于 2026-08-05 完成配置说明；具体音色待用户试听选择)*
- [x] Sentinel 构建、Node 测试、ClawBody `tests/` pytest 和浏览器布局检查通过。*(已于 2026-07-20 完成；实体 ASR/TTS/动作未纳入本次无设备验收)*
